#include <stdio.h>

int vezes_maior;
unsigned long long int exp(int base, int exp){
    vezes_maior = 0;
    unsigned long long int resultado = 1;
    int i;

    for(i = 0; i < exp; i++){
        if(resultado * base < resultado) maior = 0;
        resultado *= base;
    }
        
}

int main(){
    int base, exp, qtd, maior_i, i;
    unsigned long long int maior = 0, atual;
    int maior_i_t = 0;

    scanf("%d", &qtd);

    for(i = 0; i < qtd; i++){

        
    }

    return 0;
}